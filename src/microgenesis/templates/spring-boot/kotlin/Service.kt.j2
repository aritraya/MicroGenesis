package {{ base_package }}.service

import {{ base_package }}.dto.{{ entityName }}DTO
import java.util.List

/**
 * Service interface for {{ entityName }}.
 */
interface {{ entityName }}Service {

    /**
     * Find all {{ entityName }} entities.
     *
     * @return list of {{ entityName }}DTOs
     */
    fun findAll(): List<{{ entityName }}DTO>

    /**
     * Find {{ entityName }} by ID.
     *
     * @param id the ID to search for
     * @return the found {{ entityName }}DTO or null
     */
    fun findById(id: Long): {{ entityName }}DTO?

    /**
     * Create a new {{ entityName }}.
     *
     * @param dto the {{ entityName }}DTO to create
     * @return the created {{ entityName }}DTO
     */
    fun create(dto: {{ entityName }}DTO): {{ entityName }}DTO

    /**
     * Update an existing {{ entityName }}.
     *
     * @param id the ID of the {{ entityName }} to update
     * @param dto the updated {{ entityName }}DTO
     * @return the updated {{ entityName }}DTO
     */
    fun update(id: Long, dto: {{ entityName }}DTO): {{ entityName }}DTO

    /**
     * Delete {{ entityName }} by ID.
     *
     * @param id the ID of the {{ entityName }} to delete
     */
    fun delete(id: Long)
}
