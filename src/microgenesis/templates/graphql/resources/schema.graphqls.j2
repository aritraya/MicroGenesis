## GraphQL Schema
## Generated by MicroGenesis

schema {
  query: Query
  mutation: Mutation
}

type Query {
  {% for entity in entities %}
  {{ entity.name | lower }}(id: ID!): {{ entity.name }}
  {{ entity.name | lower }}s: [{{ entity.name }}!]!
  {% endfor %}
}

type Mutation {
  {% for entity in entities %}
  create{{ entity.name }}(input: {{ entity.name }}Input!): {{ entity.name }}!
  update{{ entity.name }}(id: ID!, input: {{ entity.name }}Input!): {{ entity.name }}!
  delete{{ entity.name }}(id: ID!): Boolean!
  {% endfor %}
}

{% for entity in entities %}
type {{ entity.name }} {
  {% for field in entity.fields %}
  {{ field.name }}: {{ field.type | replace("String", "String") | replace("Integer", "Int") | replace("Long", "Int") | replace("Double", "Float") | replace("Boolean", "Boolean") | replace("LocalDateTime", "String") | replace("LocalDate", "String") | replace("Date", "String") | replace("List<", "[") | replace(">", "!]") }}
  {% endfor %}
}

input {{ entity.name }}Input {
  {% for field in entity.fields %}
  {% if field.name != "id" %}
  {{ field.name }}: {{ field.type | replace("String", "String") | replace("Integer", "Int") | replace("Long", "Int") | replace("Double", "Float") | replace("Boolean", "Boolean") | replace("LocalDateTime", "String") | replace("LocalDate", "String") | replace("Date", "String") | replace("List<", "[") | replace(">", "!]") }}
  {% endif %}
  {% endfor %}
}
{% endfor %}
